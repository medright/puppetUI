{
  "name": "puppeteer",
  "version": "1.0.0",
  "description": "Puppeteer tests for cannabot.pro",
  "scripts": {
    "test": "jest",
    "start": "echo 'No server needed for these tests'"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.0.0",
    "jest-puppeteer": "^9.0.0",
    "puppeteer": "^21.0.0"
  },
  "jest": {
    "preset": "jest-puppeteer",
    "testEnvironment": "node"
  }
}
